on:
  workflow_dispatch:
    inputs:
      version:
        type: string
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - uses: marvjohnson/composit-action@v1
        env:
          VERSION: ${{ inputs.version }}
        with:
          version: $VERSION
